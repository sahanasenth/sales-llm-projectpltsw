# authentication/migrations/__init__.py
# Required by Django to treat the migrations directory as a Python package.
# Django auto-generates migration files here when you run:
#   python manage.py makemigrations
#
# Since we use Django's built-in User model (not a custom one),
# no migrations are needed from our app — Django handles them internally.
